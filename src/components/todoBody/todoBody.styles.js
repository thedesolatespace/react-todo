import styled from 'styled-components';

export const StyledTodoBody = styled.div`
  border-radius: 0 0 10px 10px;
  background: #ecb390;
  width: clamp(200px, 50%, 600px);
  margin: 0 auto;
  padding: 30px;
`;
